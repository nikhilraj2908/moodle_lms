<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      
        .dashboard-header {
            background-color: #0A2A4A;
            color: white;
            padding: 20px;
            display: flex;
            align-items: center;
            font-size: 20px;
        }
        .dashboard-card {
            background: white;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            text-align: center;
        }
        .user-stats img {
            width: 100%;
        }
        .chart-placeholder {
            width: 100%;
            height: 100%;
            margin: auto;
        }
        .learning-path-circles {
            display: flex;
            justify-content: center;
            gap: 20px;
        }
        .circle {
            width: 50px;
            height: 50px;
            border: 3px solid gray;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .points-bars .bar {
            height: 15px;
            background-color: lightblue;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="dashboard-header">
        {{{userpicture}}}
        <span class="ms-3">Hey! {{username}}</span>
    </div>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-3">
                <div class="dashboard-card user-stats">
                    
                    <div class="chart-placeholder">
                        <img src="{{asset1_image_url}}" alt="User Stats Chart">
                    </div>
                    <p><strong>Excellent</strong> | Good | Need to Improve</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="dashboard-card">
                    <h5>Course Enrolled</h5>
                    <h2>{{totalCourses}}</h2>
                </div>
            </div>
            <div class="col-md-3">
                <div class="dashboard-card">
                    <h5>Learning Path</h5>
                    <div class="learning-path-circles">
                        <div class="circle">✔</div>
                        <div class="circle">✖</div>
                    </div>
                    <p>Completed | Overdue</p>
                </div>
            </div>
            <div class="col-md-3">
                <div class="dashboard-card">
                    <h5>Total Earned Points</h5>
                    <div class="points-bars">
                        <div class="mb-2">July <div class="bar" style="width: {{totalPoints}}%;"></div></div>
                        <div class="mb-2">June <div class="bar" style="width: {{totalPoints}}%;"></div></div>
                        <div class="mb-2">May <div class="bar" style="width: {{totalPoints}}%;"></div></div>
                        <div class="mb-2">April <div class="bar" style="width: {{totalPoints}}%;"></div></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>